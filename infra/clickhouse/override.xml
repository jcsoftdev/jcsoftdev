<clickhouse>
  <!-- Allow default user from any network for local dev.
       In production, lock this down or use a dedicated Plausible user. -->
  <users>
    <default>
      <password></password>
      <networks>
        <ip>::/0</ip>
      </networks>
      <profile>default</profile>
      <quota>default</quota>
    </default>
  </users>
</clickhouse>
