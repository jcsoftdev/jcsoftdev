# API URL for the Hono backend (admin SPA). In dev, defaults to http://localhost:8787.
# In production builds (vite build), this MUST be set or the build will throw.
# Match this to your monorepo .env PORT (apps/api PORT setting).
VITE_API_URL=http://localhost:8787
