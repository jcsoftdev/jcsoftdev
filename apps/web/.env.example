# API URL for the public Hono backend. In dev, defaults to http://localhost:8787.
# In production builds, this MUST be set or the build will throw.
# Match this to your monorepo .env PORT (apps/api PORT setting).
PUBLIC_API_URL=http://localhost:8787
